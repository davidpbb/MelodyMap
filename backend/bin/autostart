#!/bin/bash

CURRENT_DIR=$(dirname "$0")

$CURRENT_DIR/build
$CURRENT_DIR/migrate
$CURRENT_DIR/seed
$CURRENT_DIR/fixperms
echo "Aplicación iniciada, DB creada con datos de prueba y permisos de bootstrap/cache modificados."